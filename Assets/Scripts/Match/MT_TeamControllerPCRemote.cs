using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Pit
{
    public class MT_TeamControllerPCRemote : MT_TeamControllerPC
    {
        public MT_TeamControllerPCRemote(MT_Team team) : base(team)
        {
        }

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        public override void Update()
        {
            base.Update();

        }
    }
}